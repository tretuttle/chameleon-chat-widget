import ChatWidget from "@/components/ChatWidget";

const Widget = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <ChatWidget initialState="hidden" />
    </div>
  );
};

export default Widget;
